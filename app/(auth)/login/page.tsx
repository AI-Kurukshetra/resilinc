import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login | Resilinc Lite",
};

export default function LoginPage() {
  return <main className="p-6">Login page scaffold</main>;
}
